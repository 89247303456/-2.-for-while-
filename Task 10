"""На столе лежат n монеток. Некоторые из них лежат вверх решкой, 
а некоторые – гербом. Определите минимальное число монеток, 
которые нужно перевернуть, чтобы все монетки были повернуты вверх 
одной и той же стороной. Выведите минимальное количество монет, 
которые нужно перевернуть"""

n = int(input("Введите количество монеток: ")) # Вводим количество 
#монеток

coins = [] # Создаем пустой список для хранения информации о каж  
#дой монетке
for i in range(n):
    side = input("Введите сторону монетки (решка или герб): ")
    coins.append(side) # Добавляем сторону монетки в список

flips = 0 # Переменная для подсчета количества переворотов монеток


# Проверяем каждую монетку в списке и считаем количество монеток, 
#которые нужно перевернуть
for coin in coins:
    if coin == "гербом":
        flips += 1

# Выводим минимальное количество монеток, которые нужно 
#перевернуть
print(min(flips, n - flips))
